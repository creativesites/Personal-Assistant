{
  "name": "@zuri/pdf-templates",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist && cp -r src/* dist/ 2>/dev/null || true",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@react-pdf/renderer": "^4.5.1",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
